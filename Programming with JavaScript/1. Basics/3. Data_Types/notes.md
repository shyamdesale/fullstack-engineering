# JavaScript Data Types

JavaScript is a **dynamically typed language**, meaning variables are not bound to a specific data type. The type of a variable is determined automatically at runtime.

Example:

```javascript
let value = 10;
console.log(typeof value); 

value = "JavaScript";
console.log(typeof value);