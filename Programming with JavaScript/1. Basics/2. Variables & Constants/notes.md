# JavaScript Variables and Constants

## 1. What is a Variable?

A **variable** is a container used to store data values that can be used later in a program.

Variables allow us to store information such as numbers, text, or objects and reuse them multiple times.

Example

```javascript
let age = 21;