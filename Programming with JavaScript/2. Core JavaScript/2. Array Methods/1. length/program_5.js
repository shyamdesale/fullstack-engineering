let cart = [
    "Laptop",
    "Mouse",
    "Keyboard",
    "Headphones"
];

console.log("Items in Cart :", cart.length);

if (cart.length === 0) {
    console.log("Cart is Empty");
} else {
    console.log("Ready for Checkout");
}